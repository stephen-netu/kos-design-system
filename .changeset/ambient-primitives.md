---
"@stephen-netu/design-system": minor
---

Add opt-in ambient/marketing primitives: `tokens/ambient.css` (gradient text, ambient glow, scroll-reveal), `LiveDot`, `ThemeSwatchGroup`, `WindowFrame`, `StackedCard`, and `ParticleAssembleMark`. None of these are auto-imported or wired into console/product chrome — see `AmbientPrimitivesDemo` in `src/demos/`.
